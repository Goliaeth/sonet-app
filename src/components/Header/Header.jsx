import classes from './Header.module.css'

const Header = () => {
  return (
    <header className={classes.header}>
      <img src="SoNet-App-logo.png" alt="main-logo" />
    </header>
  );
};

export default Header;
